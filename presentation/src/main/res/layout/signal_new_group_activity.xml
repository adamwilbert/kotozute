<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/Toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <View
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:background="@android:color/black" />

    <!-- Named here rather than on a second screen. Signal gives the name its own step
         because that step also carries an avatar, a timer and a list of who is in; this
         has a name and nothing else, and a screen for one field is a full repaint to
         type one word. -->
    <com.wanderwildwood.kotozute.common.widget.QkEditText
        android:id="@+id/name"
        style="@style/TextPrimary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="16dp"
        android:background="@null"
        android:hint="@string/signal_group_name_hint"
        android:inputType="textCapSentences"
        android:maxLines="1"
        android:textColorHint="?android:attr/textColorTertiary" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="?android:attr/divider" />

    <com.wanderwildwood.kotozute.common.widget.QkEditText
        android:id="@+id/search"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="4dp"
        android:background="@null"
        android:hint="@string/signal_group_search_hint"
        android:inputType="textFilter|textNoSuggestions"
        android:maxLines="1"
        android:privateImeOptions="nm"
        android:textColorHint="?android:attr/textColorTertiary"
        app:textSize="secondary" />

    <com.wanderwildwood.kotozute.common.widget.QkTextView
        android:id="@+id/empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:padding="32dp"
        android:text="@string/signal_group_nobody"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        tools:listitem="@layout/signal_group_member_list_item" />

</LinearLayout>
